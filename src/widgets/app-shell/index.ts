/**
 * Public API for the `app-shell` widget.
 *
 * Widgets compose multiple features. Only the composed surface is
 * exposed; internal sidebars are private to this widget.
 */

export { AppShell } from "./components/app-shell";
